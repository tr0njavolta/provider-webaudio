---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: sequencers.webaudio.example.com
spec:
  group: webaudio.example.com
  names:
    categories:
    - webaudio
    kind: Sequencer
    listKind: SequencerList
    plural: sequencers
    singular: sequencer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bpm
      name: BPM
      type: integer
    - jsonPath: .spec.running
      name: RUNNING
      type: boolean
    - jsonPath: .status.ready
      name: READY
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Sequencer is the top-level resource. It owns Tracks, which own
          Steps.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SequencerSpec defines the desired state of a Sequencer.
            properties:
              bpm:
                maximum: 300
                minimum: 20
                type: integer
              running:
                type: boolean
              steps:
                enum:
                - 4
                - 8
                - 16
                type: integer
            required:
            - bpm
            - running
            - steps
            type: object
          status:
            description: SequencerStatus is the observed state reported by the reconciler.
            properties:
              lastSyncTime:
                format: date-time
                type: string
              observedBPM:
                type: integer
              observedRunning:
                type: boolean
              ready:
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
