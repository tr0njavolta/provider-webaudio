---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: providerconfigs.webaudio.example.com
spec:
  group: webaudio.example.com
  names:
    categories:
    - webaudio
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serverPort
      name: PORT
      type: integer
    - jsonPath: .status.ready
      name: READY
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProviderConfig configures the Web Audio provider.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProviderConfigSpec defines configuration for the Web Audio
              provider.
            properties:
              driftCorrectionInterval:
                default: 2
                maximum: 60
                minimum: 1
                type: integer
              sampleRate:
                default: 44100
                enum:
                - 22050
                - 44100
                - 48000
                - 96000
                type: integer
              serverPort:
                default: 9090
                maximum: 65535
                minimum: 1024
                type: integer
            type: object
          status:
            description: ProviderConfigStatus reflects the observed state of the provider
              config.
            properties:
              connectedClients:
                type: integer
              ready:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
