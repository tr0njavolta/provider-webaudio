---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: steps.webaudio.example.com
spec:
  group: webaudio.example.com
  names:
    categories:
    - webaudio
    kind: Step
    listKind: StepList
    plural: steps
    singular: step
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.trackRef
      name: TRACK
      type: string
    - jsonPath: .spec.index
      name: INDEX
      type: integer
    - jsonPath: .spec.active
      name: ACTIVE
      type: boolean
    - jsonPath: .status.driftDetected
      name: DRIFT
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Step is a single cell in the sequencer grid.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StepSpec defines the desired state of a single step in the
              grid.
            properties:
              active:
                type: boolean
              index:
                maximum: 15
                minimum: 0
                type: integer
              trackRef:
                type: string
              velocity:
                default: 1
                maximum: 1
                minimum: 0
                type: number
            required:
            - active
            - index
            - trackRef
            - velocity
            type: object
          status:
            description: StepStatus is the observed state from the Web Audio engine.
            properties:
              driftDetected:
                type: boolean
              lastCorrectionTime:
                format: date-time
                type: string
              observedActive:
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
